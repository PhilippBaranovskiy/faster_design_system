/** Explicit attributes accepted by Faster UI icons. */
export interface IconProps {
  /** Additional classes applied to the SVG element. */
  className?: string;
  /** Accessible name for the icon. */
  'aria-label'?: string;
  /** References the element that labels the icon. */
  'aria-labelledby'?: string;
}
